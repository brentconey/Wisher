@model Yearnly.Web.Models.LoginModel

@{
    ViewBag.Title = "Log in";
    Layout = "~/Views/Shared/_HomePage.cshtml";
}
<div class="login group">
    <h1><a id="logo" href="/">Yearnly</a></h1>

    <div class="group" id="login_with_social">
        @Html.Action("ExternalLoginsList", new { ReturnUrl = ViewBag.ReturnUrl })
    </div>
</div>

    @*<div class="half left group" id="yearnly_login">
        <h2 class="pd">Log in</h2>

        <form data-bind="submit: logIn">
            @Html.AntiForgeryToken()
            <h3>Log in using your Yearnly account</h3>
            <label class="form-error" data-bind="visible: showAuthenticationError">Wrong username or password.</label>
            <fieldset>
                <fieldset>
                    <label for="user_name">User name</label>
                    <input type="text" data-bind="value: userName, valueUpdate: 'afterkeydown'" />
                    <label class="form-error" data-bind="visible: userNameError">Username is required.</label>
                </fieldset>
                <fieldset>
                    <label for="password">Password</label>
                    <input type="password" data-bind="value: passWord, valueUpdate: 'afterkeydown'" />
                    <label class="form-error" data-bind="visible: passWordError">Password is required.</label>
                </fieldset>
                <fieldset>
                    @Html.CheckBoxFor(m => m.RememberMe)
                    @Html.LabelFor(m => m.RememberMe, new { @class = "checkbox" })
                </fieldset>
                    
                <input type="submit" class="button right" value="Log in" />
            </fieldset>
        </form>
        <script src="~/Scripts/LandingPage/LogInModel.js"></script>
    </div>*@

    @*<div class="group half right" id="registration_form">
        <h2 class="pd">Register</h2>

        @using (Html.BeginForm("Register", "Home"))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary()
            <fieldset>
                    <label for="first_name">First name</label>
                    <input data-val="true" data-val-required="The First name field is required." id="first_name" name="FirstName" type="text" value="" />
                </fieldset>
            <fieldset>
                    <label for="user_name">Last name</label>
                    <input data-val="true" data-val-required="The Last name field is required." id="last_name" name="LastName" type="text" value="" />
                </fieldset>

            <fieldset>
                    <label for="user_name">User name</label>
                    <input data-val="true" data-val-required="The User name field is required." id="user_name" name="UserName" type="text" value="" />
                </fieldset>
                <fieldset>
                    <label for="password">Password</label>
                    <input data-val="true" data-val-length="The Password must be at least 6 characters long." data-val-length-max="100" data-val-length-min="6" data-val-required="The Password field is required." id="password" name="Password" type="password" />
                </fieldset>
                <fieldset>
                    <label for="confirm_password">Confirm password</label>
                    <input data-val="true" data-val-equalto="The password and confirmation password do not match." data-val-equalto-other="*.Password" id="confirm_password" name="ConfirmPassword" type="password" />
                </fieldset>
                <fieldset>
                    <input type="submit" id="registration_submit" value="Register" />
                </fieldset>

        }
    </div>*@
    
